# ===========================================
# ENVIRONMENT VARIABLES FOR VERCEL DEPLOYMENT
# ===========================================
# Copy this file to .env.local for local development
# In production, set these in the Vercel dashboard:
# https://vercel.com/[your-project]/settings/environment-variables
# ===========================================

# Webhook URL - where form submissions are sent
# This is your n8n or other automation webhook endpoint
# KEEP THIS SECRET! Never expose in frontend code
WEBHOOK_URL=https://webhook.ferritech.com.br/webhook/recebeDataForm

# Supabase Configuration for file storage
# Get these from Supabase Dashboard > Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_BUCKET=temp_files

# CORS Configuration (comma-separated list of allowed origins)
# Leave empty to allow all origins with Vercel domains
# For production, specify your domains:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=

# ===========================================
# SECURITY NOTES:
# ===========================================
# 1. NEVER commit .env files with real values to git
# 2. Use Vercel's encrypted environment variables
# 3. Set different values for Preview vs Production
# 4. Rotate keys periodically
# 5. SUPABASE_SERVICE_KEY has full access - keep it secret!
# ===========================================
